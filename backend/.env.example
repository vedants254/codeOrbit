# =============================================================================
# GitVizz Backend Configuration
# Copy this file to .env and update the values below
# =============================================================================

# GitHub Integration
# Used as User-Agent header when making requests to GitHub API
GITHUB_USER_AGENT=codeorbit

# JWT Authentication
# Generate a secure random string for JWT_SECRET (e.g., openssl rand -base64 32)
JWT_SECRET=your-jwt-secret-here-use-openssl-rand-base64-32
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=10080  # 7 days (7 * 24 * 60 minutes)
REFRESH_TOKEN_EXPIRE_DAYS=30  # 30 days

# Database Configuration
# MongoDB connection string and database name
MONGO_URI=mongodb://localhost:27017
MONGODB_DB_NAME=gitvizz

# Server Configuration
# Host and port for the FastAPI server
HOST=0.0.0.0
PORT=8003

# File Storage
# Base path where uploaded repositories and generated files are stored
FILE_STORAGE_BASEPATH="storage"

# Data Encryption
# Generate secure keys for data encryption (e.g., python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
# Both keys should be the same 32-byte base64-encoded string
ENCRYPTION_KEY=your-32-byte-base64-encryption-key-here
FERNET_KEY=your-32-byte-base64-encryption-key-here

# GitHub OAuth App Credentials
# Required for GitHub authentication integration
GITHUB_CLIENT_ID=your-github-oauth-client-id
GITHUB_CLIENT_SECRET=your-github-oauth-client-secret

# GitHub App Credentials
# Required for GitHub App integration (if using GitHub App instead of OAuth)
GITHUB_APP_ID=your-github-app-id
GITHUB_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----
your-github-app-private-key-here
-----END RSA PRIVATE KEY-----"

# Phoenix Observability (Optional)
# For LLM observability and tracing with Arize Phoenix
# 
# For Phoenix Cloud deployment:
# PHOENIX_API_KEY=your-phoenix-api-key
# PHOENIX_COLLECTOR_ENDPOINT=https://app.phoenix.arize.com
#
# For Local Phoenix deployment (with Docker):
# PHOENIX_COLLECTOR_ENDPOINT=http://phoenix:6006/v1/traces
# (Note: Leave PHOENIX_API_KEY empty/commented for local deployment)
#
# Uncomment and configure based on your deployment:
# PHOENIX_API_KEY=your-phoenix-api-key
PHOENIX_COLLECTOR_ENDPOINT=http://phoenix:6006/v1/traces
PHOENIX_PROJECT_NAME=gitvizz-backend
IS_DISABLING_OBSERVABILITY=true

# LLM API Keys
# Add your API keys for the language models you want to use
# At least one is required for documentation generation and chat features
OPENAI_API_KEY=your-openai-api-key-here
ANTHROPIC_API_KEY=your-anthropic-api-key-here
GEMINI_API_KEY=your-gemini-api-key-here
GROQ_API_KEY=your-groq-api-key-here